export interface PhysicalAppearance {
  readonly background: string;
  readonly digits: string;
}

export const DEFAULT_PHYSICAL_APPEARANCE: PhysicalAppearance = {
  background: '#ffffff',
  digits: '#ffffff',
};
