{
  "name": "liquid-watch-sim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check:imu": "tsc tools/replay-check.ts --outDir node_modules/.cache/replay-check --module commonjs --target es2021 --moduleResolution node --esModuleInterop && node -e \"require('fs').writeFileSync('node_modules/.cache/replay-check/package.json','{}')\" && node node_modules/.cache/replay-check/tools/replay-check.js",
    "dump:presets": "tsc tools/dump-presets.ts --outDir node_modules/.cache/dump-presets --module commonjs --target es2021 --moduleResolution node --esModuleInterop && node -e \"require('fs').writeFileSync('node_modules/.cache/dump-presets/package.json','{}')\" && node node_modules/.cache/dump-presets/tools/dump-presets.js"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
